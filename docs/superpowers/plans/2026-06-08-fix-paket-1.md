# Fix-Paket 1 (Fundament) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Den Install ehrlich und reproduzierbar machen: keine „fertig"-Lüge bei Fehlern, robuste Traffic-Endpunkte, nur-localhost-Ports, sichtbare Datenbegriffe, kein toter Legacy-Pfad.

**Architecture:** Punktuelle Korrekturen an bestehenden Dateien (`traffic/app.py`, `traffic/generator.py`, `traffic/orders.py`, `traffic/templates/index.html`, `install.sh`, `docker-compose.yml`, `wordpress/init/wp-init.sh`). Keine neuen Module. Quelle: [`review/06`](../../../review/06%20claude%20konsens-und-plan.md).

**Tech Stack:** Bash, Python/Flask, Docker Compose, MariaDB, WP-CLI.

**Test-Methodik (bewusst, Projekt-Konvention):** Dieses Repo hat keine Unit-Test-Suite; Verifikation erfolgt per Ausführen — `bash -n`, `curl` mit Soll-HTTP-Code, `python -c`/`php -l`, Container-Läufe ([HANDOFF §8](../../../HANDOFF.md)). Jede Aufgabe: Prüfung definieren (schlägt jetzt fehl) → fixen → Prüfung bestätigen → committen. **Voraussetzung:** Stack baubar (`docker compose up -d --build`).

**Reihenfolge:** Tasks 1→5 nacheinander, je 1 Commit. Danach erst Plan 2 (Fixture-Bake).

---

## File Structure

| Datei | Verantwortung / Änderung |
|---|---|
| `traffic/app.py` | `/api/ready` ehrlich (T1); tolerante Endpunkt-Parser + Bounds (T2); Besuche bei manuellen Käufen buchen + WC-Status (T4) |
| `traffic/generator.py` | `generate_orders` liefert `visits` mit (T4) |
| `traffic/orders.py` | WC-Fehler im Rückgabewert statt still `count=0` (T4) |
| `traffic/templates/index.html` | Button-Feedback bei Fehler (T2); WC-Bestellungen sichtbar trennen (T4) |
| `install.sh` | Setup-/Seed-/Archiv-Fehler ehrlich melden, Exit-Codes (T1) |
| `docker-compose.yml` | Port-Bindings auf `127.0.0.1` (T3) |
| `.env.example`, `README.md` | Ports „fix" dokumentieren (T3) |
| `wordpress/init/wp-init.sh` | Legacy-Block entfernen, fehlende Fixture → harter Abbruch (T5) |

---

## Task 1: P1.1 — Installer-Wahrheit

**Files:**
- Modify: `traffic/app.py:188-216` (`ready()`)
- Modify: `install.sh:123-124` (wait_http), `install.sh:246-257` (Seed-Warteschleife), `install.sh:260-284` (Archiv + Schlussblock)

- [ ] **Step 1: Prüfung definieren — `error` darf nicht „done" sein**

Aktuell ([`traffic/app.py:192`](../../../traffic/app.py)) gilt `error` als fertig. Soll: bei `error` → HTTP 500.
Prüfbefehl (nach dem Fix mit laufendem Stack, s. Step 6) — erst Verständnis, was falsch ist:

Run: `grep -n 'in ("done", "off", "error")' traffic/app.py`
Expected jetzt: Treffer in Zeile 192 (= der Bug).

- [ ] **Step 2: `ready()` umbauen (app.py)**

Ersetze in `traffic/app.py` die Zeile 192:

```python
    done = all(v in ("done", "off", "error") for v in seed.values())
```

durch:

```python
    failed = "error" in seed.values()
    done = all(v in ("done", "off") for v in seed.values())
```

Ersetze den Rückgabeblock (aktuell Zeilen 214-216):

```python
    summary += f" · {visits} Besuche"
    body = summary + "\n"
    return (body, 200 if done else 202, {"Content-Type": "text/plain; charset=utf-8"})
```

durch:

```python
    summary += f" · {visits} Besuche"
    if failed:
        summary = "FEHLER bei der Startbefuellung — " + summary
        code = 500
    elif done:
        code = 200
    else:
        code = 202
    body = summary + "\n"
    return (body, code, {"Content-Type": "text/plain; charset=utf-8"})
```

- [ ] **Step 3: Syntax prüfen (app.py)**

Run: `python -c "import ast; ast.parse(open('traffic/app.py').read())"`
Expected: kein Output, Exit 0.

- [ ] **Step 4: `install.sh` — Setup-Fehler nicht schlucken**

Ersetze in `install.sh` die Zeilen 123-124:

```bash
printf '   Shop   '; wait_http "http://localhost:${WP_PORT}/"        "Shop"   || true
printf '   Matomo '; wait_http "http://localhost:${MATOMO_PORT}/"    "Matomo" || true
```

durch:

```bash
SETUP_OK=1
printf '   Shop   '; wait_http "http://localhost:${WP_PORT}/"        "Shop"   || SETUP_OK=0
printf '   Matomo '; wait_http "http://localhost:${MATOMO_PORT}/"    "Matomo" || SETUP_OK=0
if [ "$SETUP_OK" -ne 1 ]; then
  echo >&2
  echo "FEHLER: Shop und/oder Matomo nicht erreichbar – Installation abgebrochen." >&2
  echo "        Logs:  ${DC[*]} logs" >&2
  exit 1
fi
```

- [ ] **Step 5: `install.sh` — Seed-Fehler/Timeout erkennen + Archiv-Ergebnis festhalten**

In der Warteschleife: Vor der Schleife (direkt nach `ROWS=4` in Zeile 223) einfügen:

```bash
  SEED_OK=1
```

Ersetze den `if [ "$code" = "200" ]`-Block + Timeout (aktuell Zeilen 246-255):

```bash
    if [ "$code" = "200" ]; then
      printf '   %s%s Startbefuellung abgeschlossen%s   (%02d:%02d)\n' "$C_GREEN" "$CHECK" "$C_RESET" "$mm" "$ss"
      break
    fi
    i=$((i + 1))
    if [ "$i" -gt 600 ]; then   # ~20 min Sicherheits-Timeout (2s-Takt)
      printf '\n      (Timeout – Befuellung laeuft im Hintergrund weiter:\n'
      printf '       %s logs -f traffic)\n' "${DC[*]}"
      break
    fi
```

durch:

```bash
    if [ "$code" = "200" ]; then
      printf '   %s%s Startbefuellung abgeschlossen%s   (%02d:%02d)\n' "$C_GREEN" "$CHECK" "$C_RESET" "$mm" "$ss"
      break
    fi
    if [ "$code" = "500" ]; then
      SEED_OK=0
      printf '   %s%s Startbefuellung FEHLGESCHLAGEN%s   (%02d:%02d)\n' "$C_YELLOW" "$WARN" "$C_RESET" "$mm" "$ss"
      printf '      %s\n' "$body"
      break
    fi
    i=$((i + 1))
    if [ "$i" -gt 600 ]; then   # ~20 min Sicherheits-Timeout (2s-Takt)
      SEED_OK=0
      printf '\n      (Timeout – Befuellung NICHT verifiziert, laeuft im Hintergrund weiter:\n'
      printf '       %s logs -f traffic)\n' "${DC[*]}"
      break
    fi
```

Ersetze den Archiv-Block (aktuell Zeilen 261-265):

```bash
  echo "      Archiviere Matomo (Berichte vorberechnen) ..."
  "${DC[@]}" exec -T matomo php /var/www/html/console core:archive \
      --force-idsites=1 --url="http://localhost/" >/dev/null 2>&1 \
      && echo "      Archivierung abgeschlossen." \
      || echo "      (Archivierung uebersprungen/fehlgeschlagen – beim ersten Bericht-Aufruf holt Matomo es nach.)"
```

durch:

```bash
  echo "      Archiviere Matomo (Berichte vorberechnen) ..."
  if "${DC[@]}" exec -T matomo php /var/www/html/console core:archive \
        --force-idsites=1 --url="http://localhost/" >/dev/null 2>&1; then
    echo "      Archivierung abgeschlossen."; ARCHIVE_OK=1
  else
    echo "      (Archivierung fehlgeschlagen – beim ersten Bericht-Aufruf holt Matomo es nach.)"; ARCHIVE_OK=0
  fi
```

- [ ] **Step 6: `install.sh` — Schlussblock mit Status + Exit-Codes**

Ersetze den gesamten Schlussblock (aktuell Zeilen 268-284):

```bash
echo
echo "============================================================"
echo "  Installation abgeschlossen. Der Stack laeuft:"
echo "    • Shop        →  http://localhost:${WP_PORT}"
echo "    • Matomo      →  http://localhost:${MATOMO_PORT}"
echo "    • Traffic Lab →  http://localhost:${TRAFFIC_PORT}"
if [ "$WAIT_SEED" -eq 1 ]; then
  echo
  echo "  Matomo ist vorbefuellt UND archiviert – Berichte stimmen sofort."
  echo "  (In Matomo ggf. Zeitraum auf die letzten ~${BACKFILL_MONTHS} Monate stellen.)"
else
  echo
  echo "  Hinweis (--no-wait): Die ${HIST_LABEL} + Bestellungen werden"
  echo "  im HINTERGRUND befuellt. Fortschritt:  ${DC[*]} logs -f traffic"
  echo "  Berichte erscheinen erst nach Befuellung + Archivierung vollstaendig."
fi
echo "============================================================"
```

durch:

```bash
echo
echo "============================================================"
echo "  Der Stack laeuft:"
echo "    • Shop        →  http://localhost:${WP_PORT}"
echo "    • Matomo      →  http://localhost:${MATOMO_PORT}"
echo "    • Traffic Lab →  http://localhost:${TRAFFIC_PORT}"
echo
FINAL_EXIT=0
if [ "$WAIT_SEED" -ne 1 ]; then
  echo "  Status: NICHT VERIFIZIERT (--no-wait) – Befuellung laeuft im Hintergrund."
  echo "  Fortschritt:  ${DC[*]} logs -f traffic"
elif [ "${SEED_OK:-0}" -eq 1 ] && [ "${ARCHIVE_OK:-0}" -eq 1 ]; then
  echo "  Status: OK – Matomo ist vorbefuellt UND archiviert; Berichte stimmen sofort."
  echo "  (In Matomo ggf. Zeitraum auf die letzten ~${BACKFILL_MONTHS} Monate stellen.)"
elif [ "${SEED_OK:-0}" -eq 1 ]; then
  echo "  Status: TEILWEISE – Befuellung ok, Archivierung fehlte (Matomo holt es beim ersten Bericht nach)." >&2
  FINAL_EXIT=2
else
  echo "  Status: FEHLGESCHLAGEN – Startbefuellung nicht erfolgreich." >&2
  echo "  Logs:  ${DC[*]} logs -f traffic" >&2
  FINAL_EXIT=1
fi
echo "============================================================"
exit "$FINAL_EXIT"
```

- [ ] **Step 7: Syntax + Happy-Path verifizieren**

Run: `bash -n install.sh && python -c "import ast; ast.parse(open('traffic/app.py').read())"`
Expected: Exit 0, kein Output.

Run (Stack frisch): `./install.sh -y; echo "EXIT=$?"`
Expected: Läuft sauber durch, Schlusszeile „Status: OK", `EXIT=0`.
Run: `curl -s -o /dev/null -w '%{http_code}\n' "http://localhost:$(grep -E '^TRAFFIC_PORT=' .env|cut -d= -f2)/api/ready"`
Expected: `200` (Seed done).

- [ ] **Step 8: Fehlerfall verifizieren (ehrlicher Exit)**

Erzeuge künstlich einen Seed-Fehler und prüfe, dass `/api/ready` 500 liefert:
Run:
```bash
TP=$(grep -E '^TRAFFIC_PORT=' .env|cut -d= -f2)
docker compose exec -T traffic python -c "import app; app.STATE['seed']['history']='error'"
curl -s -o /dev/null -w '%{http_code}\n' "http://localhost:${TP}/api/ready"
```
Expected: `500`.
> Hinweis: Der `python -c`-Eingriff wirkt nur im laufenden Prozess, wenn derselbe Interpreter bedient — alternativ den Fehlerpfad real prüfen, indem `matomo` vor dem Seed gestoppt wird (`docker compose stop matomo`) und `./install.sh -y; echo $?` einen Exit ≠ 0 mit „Status: FEHLGESCHLAGEN" zeigt. Mindestens **einer** der beiden Wege muss den 500/Exit≠0 belegen.

- [ ] **Step 9: Commit**

```bash
git add traffic/app.py install.sh
git commit -m "fix(installer): P1.1 Installer-Wahrheit – error≠done, ehrliche Exit-Codes

/api/ready liefert bei Seed-Fehler 500 statt 200/done; install.sh schluckt
Setup-Fehler nicht mehr (|| true raus), erkennt Seed-/Archiv-Fehler und meldet
Status OK/TEILWEISE/FEHLGESCHLAGEN mit passenden Exit-Codes (0/2/1).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: P1.2 — Traffic-Endpunkte robust

**Files:**
- Modify: `traffic/app.py:219-290` (`gen_visits`, `gen_orders`, `backfill`, `set_drip`) + neuer Helper
- Modify: `traffic/templates/index.html:338` (`post`), `:376-383` (Button-Handler) + CSS

- [ ] **Step 1: Prüfung definieren — Müll-Input darf kein 500 sein**

Run (laufender Stack):
```bash
TP=$(grep -E '^TRAFFIC_PORT=' .env|cut -d= -f2)
curl -s -o /dev/null -w '%{http_code}\n' -X POST -d 'count=abc' "http://localhost:${TP}/api/generate-visits"
```
Expected jetzt: `500` (der Bug — `int("abc")` wirft).

- [ ] **Step 2: Tolerant-Parse-Helper in app.py einfügen**

Füge in `traffic/app.py` direkt nach `_env_float` (nach Zeile 43) ein:

```python
def _num_arg(name, default, lo, hi, cast=int):
    """Liest request.form[name] tolerant und clampt auf [lo, hi].

    Rückgabe: (wert, fehlertext). Fehlend/leer → (default, None).
    Unparsebar → (None, "<klartext>"); der Aufrufer antwortet dann mit 400.
    """
    raw = request.form.get(name)
    if raw is None or raw == "":
        return default, None
    raw = raw.split("#", 1)[0].strip()
    try:
        val = cast(float(raw))
    except (TypeError, ValueError):
        return None, f"'{name}' muss eine Zahl sein (war: {raw!r})"
    return max(lo, min(hi, val)), None
```

- [ ] **Step 3: `gen_visits`, `gen_orders`, `backfill` absichern**

Ersetze `gen_visits` (aktuell Zeilen 219-225):

```python
@app.route("/api/generate-visits", methods=["POST"])
def gen_visits():
    count = int(request.form.get("count", 50))
    s = generator.generate_visits(count, conversion_rate=STATE["conversion_rate"])
    _accumulate(s)
    _log(f"{count} Besuche erzeugt – {s['purchases']} Käufe, EUR {s['revenue']:.2f}")
    return jsonify(s)
```

durch:

```python
@app.route("/api/generate-visits", methods=["POST"])
def gen_visits():
    count, err = _num_arg("count", 50, 1, 500)
    if err:
        return jsonify({"error": err}), 400
    s = generator.generate_visits(count, conversion_rate=STATE["conversion_rate"])
    _accumulate(s)
    _log(f"{count} Besuche erzeugt – {s['purchases']} Käufe, EUR {s['revenue']:.2f}")
    return jsonify(s)
```

Ersetze in `gen_orders` (Zeile 230) `count = int(request.form.get("count", 10))` durch:

```python
    count, err = _num_arg("count", 10, 1, 500)
    if err:
        return jsonify({"error": err}), 400
```

Ersetze in `backfill` (Zeile 244) `days = int(request.form.get("days", 90))` durch:

```python
    days, err = _num_arg("days", 90, 1, 400)
    if err:
        return jsonify({"error": err}), 400
```

- [ ] **Step 4: `set_drip` tolerant machen**

Ersetze den `with LOCK:`-Block in `set_drip` (aktuell Zeilen 269-282):

```python
    with LOCK:
        if request.form.get("visitors_per_hour") not in (None, ""):
            v = int(float(request.form["visitors_per_hour"]))
            STATE["drip_per_hour"] = max(DRIP_MIN_PER_HOUR, min(DRIP_MAX_PER_HOUR, v))
            changed.append(f"{STATE['drip_per_hour']} Besucher/Std")
        if request.form.get("conversion_rate") not in (None, ""):
            STATE["conversion_rate"] = max(0.0, min(1.0, float(request.form["conversion_rate"])))
            changed.append(f"{STATE['conversion_rate'] * 100:.1f}% Conversion")
        if request.form.get("returning_rate") not in (None, ""):
            STATE["returning_rate"] = max(0.0, min(1.0, float(request.form["returning_rate"])))
            changed.append(f"{STATE['returning_rate'] * 100:.0f}% Wiederkehrer")
        per_hour = STATE["drip_per_hour"]
        rate = STATE["conversion_rate"]
        ret = STATE["returning_rate"]
```

durch:

```python
    vph, e1 = _num_arg("visitors_per_hour", None, DRIP_MIN_PER_HOUR, DRIP_MAX_PER_HOUR)
    cr,  e2 = _num_arg("conversion_rate",   None, 0.0, 1.0, cast=float)
    ret_in, e3 = _num_arg("returning_rate", None, 0.0, 1.0, cast=float)
    for e in (e1, e2, e3):
        if e:
            return jsonify({"error": e}), 400
    with LOCK:
        if vph is not None:
            STATE["drip_per_hour"] = vph
            changed.append(f"{STATE['drip_per_hour']} Besucher/Std")
        if cr is not None:
            STATE["conversion_rate"] = cr
            changed.append(f"{STATE['conversion_rate'] * 100:.1f}% Conversion")
        if ret_in is not None:
            STATE["returning_rate"] = ret_in
            changed.append(f"{STATE['returning_rate'] * 100:.0f}% Wiederkehrer")
        per_hour = STATE["drip_per_hour"]
        rate = STATE["conversion_rate"]
        ret = STATE["returning_rate"]
```

- [ ] **Step 5: Syntax prüfen + Rebuild**

Run: `python -c "import ast; ast.parse(open('traffic/app.py').read())"`
Expected: Exit 0.
Run: `docker compose up -d --build traffic`
Expected: traffic-Container neu gebaut & gestartet.

- [ ] **Step 6: Verifizieren — kein 500, sondern 400 / geclampt**

Run:
```bash
TP=$(grep -E '^TRAFFIC_PORT=' .env|cut -d= -f2)
echo -n "müll:   "; curl -s -o /dev/null -w '%{http_code}\n' -X POST -d 'count=abc' "http://localhost:${TP}/api/generate-visits"
echo -n "leer:   "; curl -s -o /dev/null -w '%{http_code}\n' -X POST "http://localhost:${TP}/api/generate-visits"
echo -n "gross:  "; curl -s -X POST -d 'count=999999' "http://localhost:${TP}/api/generate-visits"
echo -n "drip:   "; curl -s -o /dev/null -w '%{http_code}\n' -X POST -d 'visitors_per_hour=xx' "http://localhost:${TP}/api/set-drip"
```
Expected: `müll: 400`, `leer: 200` (Default 50), `gross:` JSON mit `"visits": 500` (geclampt), `drip: 400`.

- [ ] **Step 7: UI-Feedback bei Fehler (index.html)**

Ersetze `post` (Zeile 338):

```javascript
async function post(url, data){ try{ await fetch(url,{method:'POST', body:new URLSearchParams(data||{})}); } finally{ refresh(); } }
```

durch:

```javascript
async function post(url, data){
  let ok=false;
  try{ const r=await fetch(url,{method:'POST', body:new URLSearchParams(data||{})}); ok=r.ok; }
  catch(e){ ok=false; }
  finally{ refresh(); }
  return ok;
}
```

Ersetze den Button-Handler (Zeilen 376-383):

```javascript
document.querySelectorAll('button[data-act]').forEach(b=>b.addEventListener('click',()=>{
  if(b.dataset.sent) return;
  const a=b.dataset.act;
  if(a==='visits') post('/api/generate-visits',{count:$('#m-visits').value});
  if(a==='orders') post('/api/generate-orders',{count:$('#m-orders').value});
  const lbl=b.textContent; b.dataset.sent='1'; b.textContent='Gesendet ✓'; b.classList.add('sent');
  setTimeout(()=>{ b.textContent=lbl; b.classList.remove('sent'); delete b.dataset.sent; }, 1000);
}));
```

durch:

```javascript
document.querySelectorAll('button[data-act]').forEach(b=>b.addEventListener('click', async ()=>{
  if(b.dataset.sent) return;
  const a=b.dataset.act; const lbl=b.textContent;
  b.dataset.sent='1'; b.textContent='…';
  let ok=true;
  if(a==='visits') ok=await post('/api/generate-visits',{count:$('#m-visits').value});
  if(a==='orders') ok=await post('/api/generate-orders',{count:$('#m-orders').value});
  b.textContent = ok ? 'Gesendet ✓' : 'Fehler ✗';
  b.classList.add(ok ? 'sent' : 'err');
  setTimeout(()=>{ b.textContent=lbl; b.classList.remove('sent','err'); delete b.dataset.sent; }, ok ? 1000 : 2200);
}));
```

Füge im `<style>`-Block (eine Zeile, neben den übrigen Button-Stilen) ein:

```css
button.err{ background:#fde7e9 !important; color:#b00020 !important; border-color:#b00020 !important; }
```

- [ ] **Step 8: Rebuild + Sicht-Check**

Run: `docker compose up -d --build traffic`
Expected: gebaut. (Visueller Endcheck der Button-Reaktion durch den Menschen — siehe HANDOFF §8.)

- [ ] **Step 9: Commit**

```bash
git add traffic/app.py traffic/templates/index.html
git commit -m "fix(traffic): P1.2 Endpunkte robust – tolerantes Parsen, Bounds, UI-Fehler

_num_arg() clampt count/days/vph/cr/ret und gibt bei Müll 400 statt 500;
Buttons zeigen 'Fehler ✗' bei fehlgeschlagenem Request statt blind 'Gesendet ✓'.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: P1.3 — Ports fix + nur localhost

**Files:**
- Modify: `docker-compose.yml:33`, `:89`, `:137`
- Modify: `.env.example:1-4`, `README.md` (Ports-/Zugänge-Abschnitt)

- [ ] **Step 1: Prüfung definieren — Bindings sind noch 0.0.0.0**

Run: `grep -nE '\$\{(WORDPRESS|MATOMO|TRAFFIC)_PORT\}' docker-compose.yml`
Expected jetzt: drei Zeilen ohne `127.0.0.1:` Präfix (= der Zustand vor dem Fix).

- [ ] **Step 2: Compose-Bindings auf 127.0.0.1 ziehen**

Ersetze `docker-compose.yml:33` `      - "${WORDPRESS_PORT}:80"` durch:
```yaml
      - "127.0.0.1:${WORDPRESS_PORT}:80"
```
Ersetze `docker-compose.yml:89` `      - "${MATOMO_PORT}:80"` durch:
```yaml
      - "127.0.0.1:${MATOMO_PORT}:80"
```
Ersetze `docker-compose.yml:137` `      - "${TRAFFIC_PORT}:8092"` durch:
```yaml
      - "127.0.0.1:${TRAFFIC_PORT}:8092"
```

- [ ] **Step 3: `.env.example` – Ports als „fix" dokumentieren**

Ersetze in `.env.example` die Zeilen 1-4:

```
# ----- Host-Ports -----
WORDPRESS_PORT=8090
MATOMO_PORT=8091
TRAFFIC_PORT=8092
```

durch:

```
# ----- Host-Ports (FIX: Teil der Kursumgebung; nur bewusst overriden) -----
# Gebunden an 127.0.0.1 (nur localhost erreichbar, kein LAN). Tracker-/Katalog-
# URLs gehen fest auf 8090/8091 → diese Ports nicht ohne Grund ändern.
WORDPRESS_PORT=8090
MATOMO_PORT=8091
TRAFFIC_PORT=8092
```

- [ ] **Step 4: `README.md` – Hinweis ergänzen**

Run: `grep -nE '8090|8091|8092|Port' README.md | head`
Füge im Ports-/Zugänge-Abschnitt diesen Satz hinzu (dort, wo die Ports/Logins stehen):

```
> **Ports sind fix und an `127.0.0.1` gebunden** (nur localhost, kein LAN-Zugriff). Sie gehören zur reproduzierbaren Kursumgebung – nur bewusst in `.env` überschreiben.
```

- [ ] **Step 5: Verifizieren — nur localhost**

Run: `docker compose up -d && docker compose ps --format '{{.Name}} {{.Ports}}' | grep -E '8090|8091|8092'`
Expected: jede Zeile zeigt `127.0.0.1:8090->…` etc. (nicht `0.0.0.0`).
Run: `curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8090/`
Expected: `200`/`30x`.

- [ ] **Step 6: Commit**

```bash
git add docker-compose.yml .env.example README.md
git commit -m "fix(compose): P1.3 Ports an 127.0.0.1 binden + als fix dokumentieren

Shop/Matomo/Traffic nur noch über localhost erreichbar (kein LAN). .env.example
und README erklären die Ports als fixen Teil der Kursumgebung.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: P1.4 — Datenkonsistenz sichtbar

**Files:**
- Modify: `traffic/generator.py:640-648` (`generate_orders`)
- Modify: `traffic/orders.py:66-78` (Fehler im Rückgabewert)
- Modify: `traffic/app.py` (STATE um `wc` erweitern, `gen_orders`+Drip buchen Besuche/WC-Fehler, `/api/status`)
- Modify: `traffic/templates/index.html` (eine WC-Zeile, Begriff getrennt)

- [ ] **Step 1: Prüfung definieren — manuelle Käufe erhöhen Besuche nicht**

Run (laufender Stack):
```bash
TP=$(grep -E '^TRAFFIC_PORT=' .env|cut -d= -f2)
V0=$(curl -s "http://localhost:${TP}/api/status" | python3 -c 'import sys,json;print(json.load(sys.stdin)["totals"]["visits"])')
curl -s -o /dev/null -X POST -d 'count=5' "http://localhost:${TP}/api/generate-orders"
V1=$(curl -s "http://localhost:${TP}/api/status" | python3 -c 'import sys,json;print(json.load(sys.stdin)["totals"]["visits"])')
echo "visits: $V0 -> $V1"
```
Expected jetzt: `visits` bleibt gleich (Bug: 5 Käufe ohne Besuche gebucht).

- [ ] **Step 2: `generate_orders` liefert `visits` mit (generator.py)**

Ersetze `traffic/generator.py:640-648`:

```python
def generate_orders(count, when=None):
    """Erzwingt `count` Käufe (jeweils mit vorausgehendem Besuch)."""
    catalog = _load_catalog()
    summary = {"purchases": 0, "revenue": 0.0}
    for _ in range(count):
        r = simulate_visit(catalog, when=when, force_purchase=True)
        summary["purchases"] += 1
        summary["revenue"] = round(summary["revenue"] + r["revenue"], 2)
    return summary
```

durch:

```python
def generate_orders(count, when=None):
    """Erzwingt `count` Käufe (jeweils mit vorausgehendem Besuch).

    Jeder erzwungene Kauf hat einen Besuch – daher zählt `visits` mit, damit das
    Dashboard manuelle Käufe korrekt als Besuche verbucht (Conversion bleibt stimmig).
    """
    catalog = _load_catalog()
    summary = {"visits": 0, "purchases": 0, "revenue": 0.0}
    for _ in range(count):
        r = simulate_visit(catalog, when=when, force_purchase=True)
        summary["visits"] += 1
        summary["purchases"] += 1
        summary["revenue"] = round(summary["revenue"] + r["revenue"], 2)
    return summary
```

- [ ] **Step 3: `create_orders` – WC-Fehler im Rückgabewert (orders.py)**

Ersetze in `traffic/orders.py` den `try/except` (Zeilen 66-78):

```python
    try:
        r = SESSION.post(
            f"{WP_URL}/wp-json/m392/v1/orders",
            headers={"X-M392-Key": API_KEY},
            json=payload,
            timeout=180,
        )
        r.raise_for_status()
        j = r.json()
        return {"count": int(j.get("count", 0)), "revenue": float(j.get("revenue", 0.0)),
                "returning": int(j.get("returning", 0)), "details": j.get("details", []) or []}
    except (requests.RequestException, ValueError):
        return {"count": 0, "revenue": 0.0, "returning": 0, "details": []}
```

durch:

```python
    try:
        r = SESSION.post(
            f"{WP_URL}/wp-json/m392/v1/orders",
            headers={"X-M392-Key": API_KEY},
            json=payload,
            timeout=180,
        )
        r.raise_for_status()
        j = r.json()
        return {"count": int(j.get("count", 0)), "revenue": float(j.get("revenue", 0.0)),
                "returning": int(j.get("returning", 0)), "details": j.get("details", []) or [],
                "error": None}
    except (requests.RequestException, ValueError) as exc:
        return {"count": 0, "revenue": 0.0, "returning": 0, "details": [], "error": str(exc)}
```

- [ ] **Step 4: app.py – STATE.wc, Buchung der Besuche, WC-Fehler sichtbar**

In `traffic/app.py` STATE (nach Zeile 62 `"totals": {...},`) ergänze einen WC-Block:

```python
    # Echte WooCommerce-Bestellungen (getrennt von Matomo-Traffic-Zahlen oben).
    "wc": {"orders": 0, "revenue": 0.0, "errors": 0},
```

Füge nach `_accumulate` (nach Zeile 110) einen Helper ein:

```python
def _book_wc(res):
    """Echte Shop-Bestellungen verbuchen + WC-Fehler sichtbar machen."""
    if res.get("error"):
        with LOCK:
            STATE["wc"]["errors"] += 1
        _log(f"Shop-Bestellung fehlgeschlagen: {res['error']}")
        return
    with LOCK:
        STATE["wc"]["orders"] += res.get("count", 0)
        STATE["wc"]["revenue"] = round(STATE["wc"]["revenue"] + res.get("revenue", 0.0), 2)
```

Ersetze in `_drip_worker` den Käufe-Block (aktuell Zeilen 128-133):

```python
            if s.get("purchases"):
                res = orders.create_orders(s["purchases"], days_back=0,
                                           returning_rate=STATE["returning_rate"] * 100)
                if res["count"]:
                    _accumulate({"returning": res.get("returning", 0)})
                    _log(f"{res['count']} Bestellung(en) im Shop angelegt (Live)")
```

durch:

```python
            if s.get("purchases"):
                res = orders.create_orders(s["purchases"], days_back=0,
                                           returning_rate=STATE["returning_rate"] * 100)
                _book_wc(res)
                if res["count"]:
                    _accumulate({"returning": res.get("returning", 0)})
                    _log(f"{res['count']} Bestellung(en) im Shop angelegt (Live)")
```

Ersetze `gen_orders` (aktuell Zeilen 228-239):

```python
@app.route("/api/generate-orders", methods=["POST"])
def gen_orders():
    count, err = _num_arg("count", 10, 1, 500)
    if err:
        return jsonify({"error": err}), 400
    s = generator.generate_orders(count)
    _accumulate({"purchases": s["purchases"], "revenue": s["revenue"]})
    res = orders.create_orders(count, days_back=0,                  # echte WooCommerce-Bestellungen
                               returning_rate=STATE["returning_rate"] * 100)
    _accumulate({"returning": res.get("returning", 0)})
    made = res["count"]
    extra = f" · {made} Shop-Bestellungen" if made else ""
    _log(f"{count} Käufe erzwungen – EUR {s['revenue']:.2f}{extra}")
    return jsonify(s)
```

durch (Besuche werden jetzt mitgebucht; `_num_arg`-Block aus T2 bleibt):

```python
@app.route("/api/generate-orders", methods=["POST"])
def gen_orders():
    count, err = _num_arg("count", 10, 1, 500)
    if err:
        return jsonify({"error": err}), 400
    s = generator.generate_orders(count)
    _accumulate({"visits": s["visits"], "purchases": s["purchases"], "revenue": s["revenue"]})
    res = orders.create_orders(count, days_back=0,                  # echte WooCommerce-Bestellungen
                               returning_rate=STATE["returning_rate"] * 100)
    _book_wc(res)
    _accumulate({"returning": res.get("returning", 0)})
    made = res["count"]
    extra = f" · {made} Shop-Bestellungen" if made else ""
    _log(f"{count} Käufe erzwungen – EUR {s['revenue']:.2f}{extra}")
    return jsonify(s)
```

Ergänze in `/api/status` (im `jsonify({...})`, nach `"totals": STATE["totals"],` ~Zeile 174):

```python
            "wc": STATE["wc"],
```

- [ ] **Step 5: UI – echte Shop-Bestellungen getrennt anzeigen (index.html)**

Run: `grep -nE 'totals\.|Besuche|Käufe|id="' traffic/templates/index.html | head -30`
Identifiziere die KPI-/Totals-Anzeige. Ergänze in der Render-Funktion (die `s.totals` nutzt, nahe Zeile 483 `fetch('/api/status')`) eine Zeile, die `s.wc` getrennt ausgibt, z. B.:

```javascript
  const wc = s.wc || {orders:0,revenue:0,errors:0};
  const wcEl = document.getElementById('wc-line');
  if(wcEl) wcEl.textContent =
    `Echte Shop-Bestellungen: ${wc.orders} (EUR ${wc.revenue.toFixed(2)})`
    + (wc.errors ? ` · ${wc.errors} Fehler` : '');
```

Und im Markup (bei den übrigen KPI-Zeilen) ein Ziel-Element einfügen:

```html
<div id="wc-line" class="muted">Echte Shop-Bestellungen: 0 (EUR 0.00)</div>
```

> Begriffstrennung: „Besuche/Käufe" = Matomo-Traffic (Generator), „Echte Shop-Bestellungen" = WooCommerce. Exakte Platzierung an den bestehenden KPI-Block anpassen (Look beibehalten).

- [ ] **Step 6: Syntax + Rebuild + Verifizieren**

Run: `python -c "import ast; ast.parse(open('traffic/app.py').read()); ast.parse(open('traffic/generator.py').read()); ast.parse(open('traffic/orders.py').read())"`
Expected: Exit 0.
Run: `docker compose up -d --build traffic`
Wiederhole die Prüfung aus Step 1.
Expected: `visits` steigt jetzt um 5. Zusätzlich:
```bash
curl -s "http://localhost:${TP}/api/status" | python3 -c 'import sys,json;d=json.load(sys.stdin);print("wc:",d["wc"])'
```
Expected: `wc:` zeigt `orders`/`revenue` > 0 (oder `errors`>0, falls WC nicht erreichbar — dann ist der Fehler sichtbar statt still).

- [ ] **Step 7: Commit**

```bash
git add traffic/generator.py traffic/orders.py traffic/app.py traffic/templates/index.html
git commit -m "fix(data): P1.4 Datenkonsistenz – Besuche bei Käufen, WC-Fehler sichtbar

generate_orders liefert visits mit (manuelle Käufe zählen als Besuch);
create_orders meldet WC-Fehler statt still count=0; STATE.wc trennt echte
Shop-Bestellungen von Matomo-Traffic, /api/status + UI zeigen es getrennt.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: P1.5 — Legacy-Pfad entfernen, fehlende Fixture → harter Abbruch

**Files:**
- Modify: `wordpress/init/wp-init.sh:39-293` (if-Block) und `:295-577` (Legacy-Block entfernen)

- [ ] **Step 1: Prüfung definieren — Legacy-Block existiert noch**

Run: `grep -n "LEGACY-MODUS\|Legacy-Setup\|Aufbau von Grund auf" wordpress/init/wp-init.sh`
Expected jetzt: Treffer ab Zeile ~296 (= der zu entfernende Block).
Run: `wc -l wordpress/init/wp-init.sh`
Expected jetzt: 577 Zeilen.

- [ ] **Step 2: if-Kopf → harter Abbruch bei fehlender Fixture**

Ersetze in `wordpress/init/wp-init.sh` die Zeilen 39-40:

```bash
if [ -f "$FIXTURE_DB" ]; then
  echo "[wp-init] Fixture gefunden – pruefe Restore-Marker ..."
```

durch:

```bash
if [ ! -f "$FIXTURE_DB" ] || [ ! -f "$FIXTURE_UPLOADS" ]; then
  echo "[wp-init] FEHLER: Fixture fehlt – benoetigt:" >&2
  echo "          $FIXTURE_DB" >&2
  echo "          $FIXTURE_UPLOADS" >&2
  echo "[wp-init] Kein Aufbau von Grund auf mehr (Legacy-Modus entfernt). Abbruch." >&2
  exit 1
fi

echo "[wp-init] Fixture gefunden – pruefe Restore-Marker ..."
```

- [ ] **Step 3: `exit 0` + Legacy-Block entfernen**

Der bisherige Erfolgs-`exit 0` (Zeile 292) und das schließende `fi` (Zeile 293) entfallen, weil der Block kein `if` mehr ist (der Guard oben bricht früh ab; danach läuft der Restore linear). Ersetze die Zeilen 288-293:

```bash
  # 10) Marker setzen + Erfolg melden.
  wp option update m392_fixture_restored 1 --allow-root
  echo "[wp-init] === FIXTURE-RESTORE erfolgreich abgeschlossen ==="
  echo "[wp-init] Aktive Plugins: $(wp plugin list --status=active --field=name --allow-root | tr '\n' ',')"
  exit 0
fi
```

durch:

```bash
# 10) Marker setzen + Erfolg melden.
wp option update m392_fixture_restored 1 --allow-root
echo "[wp-init] === FIXTURE-RESTORE erfolgreich abgeschlossen ==="
echo "[wp-init] Aktive Plugins: $(wp plugin list --status=active --field=name --allow-root | tr '\n' ',')"
exit 0
```

> Hinweis: Zwischen dem if-Guard (Step 2) und hier ist der Restore-Code unverändert, aber **nicht mehr eingerückt** im if-Body. Da Bash keine Block-Einrückung erzwingt, ist die bestehende 2-Space-Einrückung des Restore-Codes funktional unschädlich; optional vereinheitlichen. Entscheidend: Es gibt **kein** umschließendes `if … then` mehr.

- [ ] **Step 4: Legacy-Block (ab Zeile 295) bis Dateiende löschen**

Lösche in `wordpress/init/wp-init.sh` **alles ab Zeile 295** (`# ====…  LEGACY-MODUS …`) bis zum Dateiende (Zeile 577). Nach dem Löschen endet die Datei mit dem `exit 0` aus Step 3.

- [ ] **Step 5: Verifizieren — Syntax, Guard, Happy-Path**

Run: `bash -n wordpress/init/wp-init.sh && grep -c "LEGACY-MODUS" wordpress/init/wp-init.sh`
Expected: Syntax ok (Exit 0); `0` Treffer für LEGACY-MODUS.

Fehlende Fixture → Abbruch (Trockenlauf in Wegwerf-Container):
Run:
```bash
docker run --rm -v "$PWD/wordpress/init:/init:ro" wordpress:cli sh -c 'cd /init && bash -n wp-init.sh && echo SYNTAX-OK'
```
Expected: `SYNTAX-OK`.

Voller Lauf (Fixture vorhanden):
Run: `./install.sh -y; echo "EXIT=$?"`
Expected: läuft durch, Shop unter `:8090` erreichbar, `EXIT=0`.

- [ ] **Step 6: Commit**

```bash
git add wordpress/init/wp-init.sh
git commit -m "fix(wp-init): P1.5 Legacy-Pfad entfernt – fehlende Fixture bricht hart ab

Der Fixture-lose Aufbau-von-Grund-auf (Z.295-577) ist raus. Fehlt shop.sql.gz
oder uploads.tar.gz, bricht wp-init mit klarer Meldung und Exit 1 ab statt einen
toten Fallback zu fahren.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec-/Konsens-Abdeckung:** P1.1→T1 ✓, P1.2→T2 ✓, P1.3→T3 ✓, P1.4→T4 ✓, P1.5→T5 ✓. Alle fünf Konsens-Punkte aus [`review/06`](../../../review/06%20claude%20konsens-und-plan.md) haben eine Aufgabe.

**2. Platzhalter-Scan:** Keine TBD/„später". UI-Platzierungen in T2/T4 nennen konkreten Code + exakte Einfügeregel; einziger bewusst manueller Schritt ist der visuelle Endcheck (Token-Render-Sperre, HANDOFF §8) — explizit so dokumentiert.

**3. Typ-/Namens-Konsistenz:** `_num_arg(name, default, lo, hi, cast)` in T2 definiert, in T2/T4 identisch genutzt. `_book_wc(res)` (T4) erwartet `res["error"]`/`count`/`revenue` — geliefert von `create_orders` (T4 Step 3). `generate_orders` liefert `visits` (T4 Step 2), in `gen_orders` als `s["visits"]` genutzt (T4 Step 4). `STATE["wc"]` in Status (T4 Step 4) und UI (`s.wc`, T4 Step 5) gleich benannt. `SEED_OK`/`ARCHIVE_OK`/`SETUP_OK`/`FINAL_EXIT` in install.sh durchgängig.

**4. Abhängigkeiten:** T4 baut auf T2 auf (`gen_orders` enthält bereits den `_num_arg`-Block) — Reihenfolge T2 vor T4 zwingend; im Plan so geordnet.
