# Referenzmaterial / generierbar – nicht versionieren
matomo-latest/
matomo-latest.zip

# Lokale Konfiguration & Secrets
.env

# WordPress-Docroot (Bind-Mount): wird beim Start generiert bzw. aus der Fixture
# wiederhergestellt -> nicht versionieren. Eigene Importe landen hier lokal.
wordpress/www/

# OS
.DS_Store

# Python
__pycache__/
*.pyc

# Node
node_modules/

# Fixtures fuer reproduzierbaren Demo-Shop MUESSEN versioniert werden
# (DB-Dump + Uploads-Archiv), daher hier explizit NICHT ignorieren.
!wordpress/init/fixture/
!wordpress/init/fixture/shop.sql.gz
!wordpress/init/fixture/uploads.tar.gz
